for dir in /app/*/     # list directories in the form "/tmp/dirname/"
do
    echo check ${dir}
    dir=${dir%*/}      # remove the trailing "/"
    cd ${dir}
    git remote update && git status|grep "is behind" && git pull && docker-compose -f /docker/docker-compose.yml up --build -d ${dir##*/}
done
